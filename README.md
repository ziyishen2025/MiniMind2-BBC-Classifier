# 基于MiniMind2-Small模型微调得到的BBC Archive分类器

## 🌟 项目简介

本项目旨在利用预训练的 MiniMind2-Small 模型，针对经典的 BBC News 数据集进行微调（Fine-tuning），实现高效、准确的文本分类任务。

**主要目标：** 将新闻文本精确地分类到以下五个类别之一：`Business` (商业), `Entertainment` (娱乐), `Politics` (政治), `Sport` (体育), 和 `Tech` (科技)。

本项目采用了优化后的训练策略，成功达到了 92% 的测试集准确率，展现了 MiniMind 模型在中文 NLP 任务上的强大泛化能力。

## 🚀 项目亮点与性能

| 指标             | 结果                                                                   | 评价                             |
| :--------------- | :--------------------------------------------------------------------- | :------------------------------- |
| **测试集准确率** | **91.90%**                                                             | 性能优秀，可用于实际部署。       |
| **模型基座**     | MiniMind2-Small                                                  | 轻量、高效，适合资源受限环境。   |
| **优化策略**     | Dropout=$0.05$, $5$ Epochs                                                 | 有效缓解过拟合，提高模型鲁棒性。 |
| **主要功能**     | 文本数据预处理、模型微调、分布式训练、指标评估（F1-Score, 混淆矩阵）。 |                                  |

## ⚙️ 环境配置

本项目推荐在 Python 3.10 环境下训练

### 1. 克隆仓库
```bash
git clone https://github.com/ziyishen2025/MiniMind2-BBC-Classifier
```

### 2. 环境搭建
```bash
pip install -r requirements.txt
```

### 3. 数据准备
请在`jingyaogong`的项目中下载[微调后的MiniMind2-Small模型](https://www.modelscope.cn/models/gongjy/MiniMind2-PyTorch/files)，下载后放在`./out`目录下。

本项目使用了Kaggle上的[BBC News Archive](https://www.kaggle.com/datasets/hgultekin/bbcnewsarchive?resource=download)数据集，下载后请放在`./dataset`目录下。

## 🎬 开始训练

模型参数和数据集准备好后，使用下面的命令开始微调训练。脚本将自动处理数据加载、模型初始化，并在训练结束后进行测试集评估。
```bash
python train_classification.py --epochs 5 --batch_size 32 --learning_rate 2e-5 --data_path "../dataset/bbc-news-data.csv" --num_labels 5 --from_weight "full_sft" --device "cuda:0" --log_interval 10
```

# 致谢、引用与许可证
## 致谢
本项目基于`jingyaogong`的`MiniMind`项目代码进行修改和优化
## 引用
如果您在研究或工作中使用本项目（MiniMind2-Small BBC 文本分类器）或基于本项目代码继续开发，请引用原始 MiniMind 项目。

请使用以下 BibTeX 格式进行引用：

```bibtex
@misc{minimind,
  title={MiniMind: Train a Tiny LLM from scratch},
  author={Jingyao Gong},
  year={2024},
  howpublished={[https://github.com/jingyaogong/minimind](https://github.com/jingyaogong/minimind)}
}
```
## 许可证
本项目遵循Apache License 2.0许可证。您可以在项目根目录下的`LICENSE.txt`文件中查阅完整的许可证文本和相关权利义务。